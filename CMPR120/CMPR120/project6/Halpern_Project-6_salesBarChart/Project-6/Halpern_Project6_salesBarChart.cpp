ZJDJnhgUEotXtAj2uvZQDc4GMcEeh4k%2BKjqICmwAZ5X7wcyo3e%2FFwlWQbkiumnC06FepJx1PFqqIYo3sYo99mBW4KY9puWt2UVxxpI3K1u5mq%2BYEzr62F7rwFR71J1gWbDAbqL16ICtiuts7LN2Z2mw%2BsEUranhx5I5hmbNlTV%2FFBue5eSD5Xzc9e9aW76eH512FzEp9%2FY9d8%2Bg8%3D
* 
* 
* 
* //http://www.cplusplus.com/articles/j3wTURfi/ article on why not to use system()
*	- its resource heavy
*	- it bypasses many antivirus programs since it makes calls directly to the operating system
*	- and more
*	alternatively use cin.get()
*
* //http://www.cplusplus.com/articles/4z18T05o/ article on how to clear the screen (includes system() method as well as non system() method)
	
	void ClearScreen()
    {
    cout << string( 100, '\n' );
    }

	to get a input from a file with a delimeter use getline, to ignore up to a delimeter use in.ignore(char #, delim )
* 
* https://www.tutorialspoint.com/the-best-way-to-check-if-a-file-exists-using-standard-c-cplusplus
*	read this to figure out how to check if a file exists, however opening the book i saw fstream and I may default to using that instead because it's included in the frstream library
* 
* 
* https://stackoverflow.com/questions/10314682/user-inputcin-default-value
*	trying to figure out how to have it if the user enters a blank string, the value is set to a default
*		this led me to https://www3.ntu.edu.sg/home/ehchua/programming/cpp/cp10_IO.html#zz-1.4
*			introduces istream and ostream which are defined as ostream being "typedef of basic_ostrem<char>" and istream being "istream class is a typedef to basic_istream<char>"
*				about typedef see: https://www.codesdope.com/cpp-typedef/#:~:text=typedef%20in%20C%2B%2B,hectic%20for%20some%20of%20us.
*			we have used istream in class without explicitly saying that we are using istream ( ie: cin.get(), cin.ignore(), getline() and cin.getline() )
*				">>" and "<<" are stream extraction and stream insertion of formatted data. It takes the data from the iostream and carries them to the desired location in the desired format.
*				get(),getline(), and read() are unformatted data types that capture the bytes and leaves them as they are instead of converting them to formatted data
*					for more info about read(): https://www.cplusplus.com/reference/istream/istream/read/
*				the istream class overides the stream insertion and stream extraction operators ( but this isnt needed for what I am trying to do at the moment )
*				"<ios>, <istream>, <ostream> and <streambuf>" are all subclasses of the <iostream> library, so no new libraries need to be included
*	
		the stackoverflow post leads me to believe that the converting both an input into an int and getting a blank line is possible with the basic_isstringstream ( which is part of std ), leading me to believe that 
		I wont need to include any more packages.
*	THIS IS MISTAKEN ^^ it seems I will need to include the <sstream> library to do the desired conversion from a string to an int
*		for more infromation about istringstream see: https://www.cplusplus.com/reference/sstream/istringstream/istringstream/
* for implimentation in code see lines: 245-250, 
* 
* 
* note: many of the loops that cycle through the array are succeptable to a buffer overflow and if too many lines are in the file and dont correspond to the amount spaces the array holds.
	Another possible future practice may be to have a function to count the amount of lines within the file before creating the array.
* 
*/

#include <iostream>												//input output stream. Preprocessor command to include the input output stream library 
#include <fstream>												//file input stream
#include <string>												//string library
#include <iomanip>												//used for manioulating input output ( this is needed for setw() and left and right )
#include <sstream>												//needed for istringstream moving characters in the input string buffer (converting it) to an int
//#include <cstdlib>											//not used and when included casues the cout and cin to be marked by intellisence as missing 
using namespace std;											//using the standard library ( allows to display simply cout and cin instead of std::cout or std::cin )


void Clear(){//will "clear" the screen by pushing the previous output above the view of the console
	cout << string(100, '\n');									//will print \n 100 times and clear the screen
	//can set other values other than 100, for this program 50 also works as well as 30, 10 and 20 are too small
}


int main() {//main class
	

	//Declaration of variables:
	//initialization and instatiation.
	const int ARRAY_SIZE = 5;		//constant value with the values of five to be used to set the size of the array
	int fileLength = int();			//for possible future use to count the amount of lines that are within the file
	const char STAR = '*';			//each star represents 100$
	int sales[ARRAY_SIZE] = {};		//creates an array of the sales values, can have the size be determined by the user as well.
	string labels[ARRAY_SIZE] = {};	//creates an arraty of the labels found
	string header = string();		//if header exits it is loaded into this variable
	string inFileName = string();	//file name for ifstream
	string outFileName = string();	//file name for ofstream
	ifstream in = ifstream();		//file input object
	ofstream out = ofstream();		//file output object
	fstream read = fstream();		//read, write, create, files
	string inputBuffer = string();	//will use this string instead of the char below to convert the inputted value to an int if needed
	//char temp = char();			//temp char to see if a blank space or an enter was inputted //this will not be used, since I will use a string as a buffer instead in combination with the istringstrem class
										//istringclass is a subclass of iostream and part of the <sstream> library
	int choice = int(1);			//a variable that will be loaded with the users selected choice, defaults the initialization to 1 to pass the first do-while nested if statement
	int count = int(0);				//counter object, loop controler variable
	int append = int();
									//int(n) where n is a initial value is an example of an overloaded constructor. int() is the default constructor, and int(n) assigns that value as the initial value ( this is the same I belive as int x = 1; )
	//End Declaration of variables

	//################################################################################################################################################
	//Homework Header
	//################################################################################################################################################

	//program header
	cout << "=====================================================================" << endl;
	cout << endl;									//blank line for readability
	cout << "Author's Name: Ben Halpern" << endl;
	//displays Authors Name, followed by an end-line command, printing the next line on a new line
	cout << "Program Name :  Halpern_Project6_salesBarChart.cpp" << endl;
	//displays Program Name, followed by an end-line command, printing the next line on a new line
	cout << "Date : October 12, 2020 " << endl;
	//display's date, followed by an end-line command, printing the next line on a new line
	cout << "College Name : Santa Ana College(SAC)" << endl;
	//displays College Name, followed by an end-line command, printing the next line on a new line
	cout << endl;									//blank line for readability
	cout << "=====================================================================" << endl;
	cout << endl;

	//Algorithm:
	//flow chart: link in notes above
	//ask if user would like to input the data, or use data from a file.
		//if would like to get data from a file:
			//ask user to specify the file name 
			//ask user if file has labels
			//ask user if file has a header
			//read file and load variables
			//ask if user wants to display to screen or to screen and to file
				//if user wants to write to file:
					//ask for a file name
					//check if file exits
					//if file exists:
						//ask user if wants to append or overwite
					//if file doesn't exist, creates file
				//display based off of the inputed file
			//read:
			//use the iftream >> to read the data, note you can use getline(in, variable, 'delimeter') to read until a delimeter
		//else prompt the user to input the values of sales to be used to dislay the bars
	//Execute output procedure based on user's request
			//write/append
			//use the ofstream << to write to file and ofsteam::out | ofstream::append to append to file

	//################################################################################################################################################
	//queries user
	//################################################################################################################################################

	//gives user the options to be queried for results or read the results from a file
	//utilizing the input output file stream 
	do {//asks the user for their preferred input method
		if (choice != 1 && choice != 2)
			cout << "Please input one of the options ( 1 or 2 )" << endl;
		cout << "Would you like to input the sales data or get the data from a file" << endl;
		//prints out the options using printf which formats the output , the first field formatted with left justification with a width of 22, and the second with a width of 30
		//printf("%-22s %30s","1: input sales data","2: input from file");	//does the same thing as below
		cout << setw(22) << left << "1: input sales data" << setw(30) << right << "2: input from file";
		cout << endl << "Your choice (1, 2):";
		cin >> choice;
	} while (choice != 1 && choice != 2);//loops as long as choice is not 1, or 2
	//end do while

	//cleans input stream of any bad or leftover values
	//cin.ignore();												//ignores any characters in the input stream (istream) buffer			
	cin.clear();												//clears any characters in the input stream (istream) buffer
	//end cleaning input stream leftover data
	//the following will "clear" the screen by pushing the previous output out of current console view
	//Clear();
	
	cout << endl;					//blank line for visibility
	cout << endl;					//blank line for visibility
	cout << endl;					//blank line for visibility

	//gets information about choices
	//conditional to run each set depending on the users input
	if (choice == 1) {//manual input sales data


		//################################################################################################################################################
		//asking user to manually input data
		//################################################################################################################################################

		while ((count < ARRAY_SIZE) && ((cout << "Enter today's sale for store " << count + 1 << ": ") && cin >> sales[count])) {
																//the conditions within the loop will print out the prompt ( if successful will be evaluated as true)
																//then will request the users input, and if succesful ( if successful will be evaluated as true)
																//if the loop will also loop as long as the count is below the array size
			//cin >> sales[count];								//placed inside of the loop to check if the user is at the end of the file
			count++;											//increments count by 1
		}//end while
		
		//put after every input stream is used to clear bad characters before the next
		//cleans input stream of any bad or leftover values
		cin.ignore();											//ignores any characters in the input stream (istream) buffer			
		cin.clear();											//clears any characters in the input stream (istream) buffer
		//end cleaning input stream leftover data
		
		/*
		 //this is an example using a do-while loop
		count--; //sets the count to be -1 prior to starting the loop for the first itteration of the do while it will be converted to 0 and then incremented
		do {
			cout << "Enter today's sale for store " << count + 1 << ": ";
			count++;
		} while (cin >> sales[count] && count < ARRAY_SIZE);
		*///end do while example
		
		
		//this is an example using a for loop
		/*for (count = 0; (count < ARRAY_SIZE) && ((cout << "Enter today's sale for store " << count + 1 << ": ") && cin >> sales[count]); count++);
																//single line loop because all that needs to be done is done within the conditions of the loop
																//the conditions of the loop check if the output statement was succesful and if the the input from the user was of the correct
																//data type and loaded into the variable correctly
		//end for loop example*/



	}//end if
	else if (choice == 2) {//read sales data from a file
		//localized variable instantiation and initialization
		int fileTChoice = int(1);								//local variable that only exists within the if statement	//for the type of data within the file, default 1
		int fileHChoice = int(1);								//local variable that only exists within the if statement	//if the file has a header within the file, default 1
		char delim = char(' ');									//local variable that only exists within the if statement	//delimeter before first numerical data type
		//end localized variable instantiation and initialization
		
		//################################################################################################################################################
		//file input
		//################################################################################################################################################
		
		cout << "Please input a file name in which you would like to read the data from:" << endl;
		cin >> inFileName;

		in.open(inFileName);									//opens file


		//cleans input stream of any bad or leftover values
		cin.ignore();											//ignores any characters in the input stream (istream) buffer			
		cin.clear();											//clears any characters in the input stream (istream) buffer
		//end cleaning input stream leftover data

		do {//this outer do-while is in place to send the program back to the top if the else of the conditional statement is triggered in which the user is asked to input a valid file
			//sending the user back to the start of the loop but this time with a valid file name to be passed through the if condition. Upon completion of the if condition a break steament exits the do-while
			if (in) {//checks to see if the file exists
				//if file exists:
				//asks user to determine the format of the data within the file
				do {//do while loop for when the values are not of the two options

					cout << endl;			//blank line for visibility

					if ((fileHChoice != 1 && fileHChoice != 2))	//conditional to query user to enter valid option
						cout << "Please enter a valid option";

					cout << "Please specify if the file is purely numerical data, or contains labels as well.\n"
						<< "An example is shown below:" << endl;
					cout << "\tPurely numerical data:\n"
						<< "\t\t900\n\t\t800\n\t\t7000\n\t\t1800\n";
					cout << "\tWith labels:\n"
						<< "\t\tStore 1:\t 900\n"
						<< "\t\tStore 2:\t 800\n"
						<< "\t\tStore 3:\t 7000\n"
						<< "\t\tStore 4:\t 1800\n";

					cout << endl;			//blank line for visibility

					cout << setw(22) << left << "1: Purely numerical data" << setw(30) << right << "2: With labels";
					cout << endl << "Your choice (1, 2) (default: 1):";

					//cleans input stream of any bad or leftover values
					//cin.ignore();											//ignores any characters in the input stream (istream) buffer			
					cin.clear();											//clears any characters in the input stream (istream) buffer
					//end cleaning input stream leftover data

					//if input is given then loads input into fileTChoice, else the default value remains
					getline(cin, inputBuffer);					//gets the (raw) unformatted input from the user and loads it into the string inputBuffer
					if (!inputBuffer.empty()) {//if the inputBuffer is not empty 
						istringstream buffer(inputBuffer);		//instatiates the buffer object of type istringstream and moves the input (string) stream buffer to the object
						buffer >> fileTChoice;					//then uses the buffer from the input stream and formatts it into the variable using the stream extraction operator ">>"
					}//end if 
					//else ( the user inputed no value other than enter or blank spaces): the value of the fileTChoice will remain as its default

					//reset inputBuffer
					inputBuffer = string();
					//cleans input stream of any bad or leftover values
					//cin.ignore();								//ignores any characters in the input stream (istream) buffer			
					cin.clear();								//clears any characters in the input stream (istream) buffer
					//end cleaning input stream leftover data

					////figure a different way how to do this ( different way is above )
					//if (cin.get(temp)) {
					//	cout << "temp " << temp;	 //debugging
					//	if (temp == '\n' || temp == ' ')
					//		break;
					//	else if (temp == '1')
					//		fileTChoice = 1;
					//	else if (temp == '2')
					//		fileTChoice = 2;
					//}//end if

					//cout << endl << fileTChoice;				//debugging

					
				} while ((fileTChoice != 1 && fileTChoice != 2));//loops as long as fileTChoice are not of the values of 1, 2
				//end of do-while loop

				//further file formatting queries ( whether the file contains a header or not )
				do {//do while loop for when the values are not of the two options
					cout << endl;			//blank line for visibility
					cout << endl;			//blank line for visibility

					if ((fileHChoice != 1 && fileHChoice != 2))	//conditional to query user to enter valid option if non valid option was previously entered
						cout << "Please enter a valid option";	//this is passed over the first time because fileHChoice has a default initialization of 1

					cout << "Please specify if the file contains a Header with in the file.\n"
						<< "An example is shown below:" << endl;
					cout << "Please specify if the file is purely data, or contains labels as well.\n"
						<< "An example is shown below:" << endl;
					cout << "\tNo Header:\n"
						<< "\t\t900\n";
					cout << "\tWith Header:\n"
						<< "\t\t\t HEADER \t\t\n"
						<< "\t\tStore 1:\t 900\n";

					cout << endl;			//blank line for visibility

					cout << setw(22) << left << "1: No Header" << setw(30) << right << "2: With Header ";
					cout << endl << "Your choice (1, 2) (default: 1):";
					
					//reset inputBuffer
					inputBuffer = string();
					//resets stream enviornment
					//cleans input stream of any bad or leftover values
					//cin.ignore();								//ignores any characters in the input stream (istream) buffer			
					cin.clear();								//clears any characters in the input stream (istream) buffer
					//end cleaning input stream leftover data

					//in future can create a function to do this to shorten code ( for now we want to see whats going on )
					//if input is given then loads input into fileHChoice, else the default value remains
					getline(cin, inputBuffer);					//gets the (raw) unformatted input from the user and loads it into the string inputBuffer
					if (!inputBuffer.empty()) {//if the inputBuffer is not empty 
						istringstream buffer(inputBuffer);		//instatiates the buffer object of type istringstream and moves the input (string) stream buffer to the object
						buffer >> fileHChoice;					//then uses the buffer from the input stream and formatts it into the variable using the stream extraction operator ">>"
					}//end if 
					//else ( the user inputed no value other than enter or blank spaces): the value of the fileTChoice will remain as its default

					//reset inputBuffer
					inputBuffer = string();
					//cleans input stream of any bad or leftover values
					//cin.ignore();								//ignores any characters in the input stream (istream) buffer			
					cin.clear();								//clears any characters in the input stream (istream) buffer
					//end cleaning input stream leftover data

					////figure a different way how to do this
					//if (cin.get(temp)) {
					//	cout << "temp " << temp;		 //debugging
					//	//cleans input stream of any bad or leftover values
					//	cin.ignore();
					//	cin.clear();
					//	//end cleaning input stream leftover data
					//	//cin.get();					 //debugging
					//	if (temp == '\n' || temp == ' ')
					//		break;
					//	else if (temp == '1')
					//		fileTChoice = 1;
					//	else if (temp == '2')
					//		fileTChoice = 2;
					//}//end if

					//cout << "\ntemp \'" << temp << "\'" << endl;//debugging
					//cout << fileHChoice << "File H Choice" << endl; //debugging

				} while (fileHChoice != 1 && fileHChoice != 2);//loops as long as the values of fileHChoice are not 1, 2
				//end do while

				//cout << "moving into switch " << fileTChoice << " : " << fileHChoice << endl; //debugging

				//pick the actions to take based on the file type (pureley numerical, or with labels)
				switch (fileTChoice) {
				case (1):										//purely data
					cout << "In case 1" << endl;//debugging
					//################################################################################################################################################
					//reading file
					//################################################################################################################################################

					if (fileHChoice == 2)						//with header 
						getline(in, header, '\n');				//gets the whole first line up until the line break
					for (int i = 0; in >> sales[i] && !(in.eof()) || i < ARRAY_SIZE; i++) {//loops to load the data to the file, as long as the data is of the proper data type and  is not end of file
						cout << "Read line" << endl;//<< (i + 1) << endl;	//for debugging purposes
						cout << sales[i] << endl;//debugging
					}//end for
					//cout << "Read last line" << endl;			//for loop exits, telling user that the for loop exited and reached EOF, and or there was nothing left for in >> sales[i] to load.
					cout << endl;			//blank line for visibility
					break;										

				case (2):										//with labels
					//cout << "In case 1" << endl;//debugging
					//reset inputBuffer
					inputBuffer = string();
					//resets stream enviornment
					//cleans input stream of any bad or leftover values
					//cin.ignore();								//ignores any characters in the input stream (istream) buffer			
					cin.clear();								//clears any characters in the input stream (istream) buffer
					//end cleaning input stream leftover data

					//queries user for delimeter if one exists
					cout << "Please specify a delimeter if one exists for the labels\n"
						<< "An example is shown below:" << endl;
					cout << "label 1: 1234" << setw(25) << right << "The delimeter is ':' " << endl;
					cout << "Delimeter (default: blank spaces ): ";

					getline(cin, inputBuffer);
					if (!inputBuffer.empty()) {//if the inputBuffer is not empty 
						cout << "The input buffer is : \'" << inputBuffer << "\'" << endl;
						istringstream buffer(inputBuffer);		//instatiates the buffer object of type istringstream and moves the input (string) stream buffer to the object
						buffer >> delim;						//then uses the buffer from the input stream and formatts it into the variable using the stream extraction operator ">>"
					}//end if 

					//reset inputBuffer
					inputBuffer = string();
					//resets stream enviornment
					//cleans input stream of any bad or leftover values
					//cin.ignore();								//ignores any characters in the input stream (istream) buffer			
					cin.clear();								//clears any characters in the input stream (istream) buffer
					//end cleaning input stream leftover data

					cout << "The delimeter is : \'" << delim << "\'" << endl;

					cout << endl;			//blank line for visibility
					
					//################################################################################################################################################
					//reading file
					//################################################################################################################################################

					//reads data from the file
					if (fileHChoice == 2)	//with header 
						getline(in, header, '\n');//in.ignore(1, '\n');//ignores first line of the file
					for (int i = 0; ((getline(in, labels[i], delim) && in >> sales[i]) && !(in.eof())) || i < ARRAY_SIZE; i++) {//loops to load the data to the file, as long as the data is of the proper data type and  is not end of file
						cout << "\nRead line";//<< (i + 1) << endl;//for debugging purposes
						//cout << setw(20) << right << "Itterator value" << i << endl;
					}//end for
					//cout << "Read last line" << endl;		   //for loop exits, telling user that the for loop exited and reached EOF, and or there was nothing left for in >> sales[i] to load.
					
					//################################################################################################################################################
					//outputting data to screen
					//################################################################################################################################################
					cout << endl;			//blank line for visibility
					cout << "outputting the contents of the file to the screen ... " << endl;
					cout << endl;			//blank line for visibility
					//output the file data to the console
					
					if (fileHChoice == 2)	//if with header print out the header
						cout << setw(50) << right << header << endl;
					for (int i = 0; i < ARRAY_SIZE; i++ ) {
						cout << labels[i] << " - " << sales[i] << endl;
					}//end for
					break;
				}//end switch
				break;											//breaks out of the outer do -while loop
			}//end if 
			//else if the reading of the file fails
			while (in.fail()) {//loops while the file is not found
				cout << "Please specify another file. " << endl;
				cout << "The file at location \"" << inFileName << "\" can't be found.";
				cout << "Try specifying the full file path of the file if it is not within the same directory as the current running program." << endl;
				cout << endl;				//blank line for visibility
				cout << "Please input a file name in which you would like to read the data from:" << endl;
				cin >> inFileName;
				in.open(inFileName);
				cout << inFileName;
				
				cin.clear();									//clears characters from input stream
			}//end while
		}while (in);//if the above while function enters a second proper input, send the program back to the top to complete the above if statement, then break the loop

		
	}//end else if 
	else {//trailing else condition to cathch answers other than 1, or 2 that passed the first do-while ( error catching )
		cout << "Your choice was neither 1 or 2.\n There seems to be an error in the first block of code up to this point" << endl;
	}//end else

	in.close();													//close input file stream

	//################################################################################################################################################
	//begining of output 
	//################################################################################################################################################
	
	//reset/reassigns choice varaible to 1
	choice = 1;
	//query the user if they would like to output the data to a screen as well
	cout << "Please specify if you would like to output the data to a file as well. \nTo the screen only or Also to a file" << endl;
	cout << setw(22) << left << "1: Also to a file" << setw(30) << right << "2: Not to a file ";
	cout << endl << "Your choice (1, 2) (default: 1):";

	cin >> choice;
	//clears screen: //see function above and notes in the header of the program
	//Clear();

	cin.clear();												//clears characters from input stream

	if (choice == 1) {//also to file
		//check if file exists
		cout << "Please specify name of the file to write to: \n";
		cin >> outFileName;
		read.open(outFileName);//could also use the 'in' object from before to check, but I wanted to use the fstream object ( both to check functionality and to create a seperate varaible for readability ) 
		if (read) {//if read is true ( file exists )
			do {//loops when fails to load input to append and the value is a number other than 1 or 2
				cout << "File already exits, would you like to append: \n"
					<< setw(10) << left << "1: Yes" << setw(10) << right << "2: no" << endl;
				cout << "Please input either \'1\' or \'2\'" << endl;
				cin.clear();									//clears characters from input stream
			} while ((cin >> append) && (append != 1 && append != 2));
			if (append == 1)
				out.open(outFileName, ofstream::out | ofstream::app);//appends to file
			else
				out.open(outFileName);							//overwrites file
			cin.clear();										//clears characters from input stream
		}//end if
		else
			out.open(outFileName);								//creates new file
		read.close();//close read

	}//end if

	cout << endl;					//blank line for visibility
	
	//header of output
	cout << endl << "SALES BAR CHART" << endl;
	if (choice == 1)
		out << endl <<"SALES BAR CHART" << endl;
	cout << "(Each * = $100)" << endl;
	if (choice == 1)
		out << "(Each * = $100)" << endl;
	//interpreting the data
	//reset count (loop controler variable to 0)
	count = 0;
	while (count < ARRAY_SIZE) {//loops while the count is below the array size (5)
		int count2 = 0;				//local count2 variable, exists only within the loop
		cout << "Store " << count + 1 << ": ";
		if (choice == 1)
			out << "Store " << count + 1 << ": ";
		while (count2 < (sales[count] / 100)) {//loops while the count2 is below the sales value divided by 100
			cout << STAR;
			if (choice == 1)
				out << STAR;
			count2++;

		}//end while


		////for loop example
		//for (int i = 0; i < (sales[count] / 100); i++) {//loops for when i is below the sales value divided by 100
		//	cout << STAR;
		//	if (choice == 1)
		//		out << STAR;
		//}//end while

		////alternative method: ( uses setfill() and setw() to create a width of the value of stars and outputs them to the screen
		//int val = (sales[count] / 100); //local variable within the loop
		//cout << setfill(STAR);
		//if (choice == 1)
		//	out << setfill(STAR);
		//cout << "Store " << count + 1 << ": " << setw(val) << "";
		//if (choice == 1)
		//	out << "Store " << count + 1 << ": " << setw(val) << "";

		count++;
		cout << endl;
		if (choice == 1)
			out << endl;
		//system("pause");//debugging	//see notes in the header of the program for information about system() calls	
	}//end while

	//resets inputBufffer
	inputBuffer = string();
	read.open(inFileName);//starts from beigining of file
	if (read) {//if in exists
		out << endl;					//blank line for visibility
		out << endl;					//blank line for visibility
		out << setw(80) << right << "This chart was produced from data from: " << inFileName << ":" << endl;	//display the original file name as a header
		while (!read.eof()) {//loops until reaches the end of the file
			getline(read, inputBuffer);//loads whole line into inputBuffer
			out << inputBuffer << endl;
			//cout << in.peek();//debugging displays the input stream to console
		}//end while
		
	}//end if

	//clears input stream and resets inputBuffer
	cin.ignore();												//ignores any characters left in the input stream
	cin.clear();												//clears input file stream
	inputBuffer = string();
	//end clears input stream and resets inputBuffer
	if (choice == 1) {//if into file, add additional notes
		//custom message appended to end of file
		cout << "Please enter any additional notes to be appended to the file: (Press ENTER when done)" << endl;
		getline(cin, inputBuffer);//captures blank spaces too
		out << "\n\nNOTES: " << inputBuffer;
	}//end if
	//displaying report out to the screen as it gets printed into a file

	//closes open fstream objects and asks user to press enter to exit
	read.close();												//close read file stream
	out.close();												//closes output file stream
	//cin.ignore();												//ignores any characters left in the input stream
	cin.clear();												//clears input file stream
	cout << "Press ENTER to exit . . .";
	cin.get();
	cin.clear();												//clears input file stream
	return 0; //returns to the system 0 if succesfull , not neccesary for int main () but it is good practice

}//End of main()
//EOF
